server:
  port: 9998
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/beacon89/springcloud/
          search-paths: config-repo
          username: beacon
          password: 820809
          basedir: ${user.home}/git/springcloud
          default-label: master
          clone-on-start: false
          timeout: 300
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:9999/eureka/
